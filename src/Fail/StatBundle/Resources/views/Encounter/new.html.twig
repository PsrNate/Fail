<h1>Encounter creation</h1>

<form action="{{ path('create_encounter') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<a href="{{ url('admin_index') }}">Back to admin index</a>