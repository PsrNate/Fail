<h1>Encounter creation</h1>

<form action="{{ path('encounter_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<a href="{{ url('index_admin') }}">Back to admin index</a>