<h1>Player list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Elo</th>
            <th>Matches</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('player_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.elo }}</td>
            <td>{{ entity.matchesCount }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('player_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('player_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('player_new') }}">
            Create a new entry
        </a>
    </li>
    <li>
        <a href="{{ url('admin_index') }}">
            Back to admin index
        </a>
    </li>
</ul>
